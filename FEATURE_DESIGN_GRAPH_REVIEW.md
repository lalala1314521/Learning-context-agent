# 脉络图 & 出题复习 · 功能设计优化方案（设计层面）

> 版本: v1.0  |  日期: 2026-08-10
> 范围: 产品 / 体验 / 交互 / 数据模型设计，不含工程稳定性
> 关联: 视觉已按"青春活力风"定稿（掌握度圆环、连击、今日目标、成就徽章），本文设计与之逐点对应

---

## 一、设计理念：从"静态笔记"到"活的复习机器"

当前脉络图与复习是两个孤立的工具：图是"看"的，题是"背"的。本次设计的核心转变是把它们整合为**一个闭环学习系统**：

```
         ┌─────────────── 知识空间（脉络图）───────────────┐
         │  空间维度：概念如何组织、如何关联                  │
         │  ↑ 答错时定位薄弱点           ↓ 生成后自动出题     │
         └────────────────────────────────────────────────┘
                     │                                │
                     ▼                                ▼
         ┌─────────────── 时间调度（复习系统）──────────────┐
         │  时间维度：何时复习、复习什么、掌握到什么程度        │
         └────────────────────────────────────────────────┘
```

- **脉络图是知识的结构化空间**：回答"这些概念是什么、彼此什么关系"
- **复习是知识的时间化调度**：回答"什么时候该巩固、哪个概念还没掌握"
- **两者互相驱动**：图决定出什么题；复习结果反哺图（薄弱节点高亮、掌握度上色）

一句话设计主张：**用户打开产品时看到的不是"一张图 + 一堆题"，而是一个知道自己哪里没学会、并且能带着他学会的知识伙伴。**

---

## 二、脉络图功能设计

### 2.1 视图体系：一图多面（当前只有 Mermaid + 大纲）

同一份节点数据，按内容性质提供不同"镜头"：

| 视图 | 适用内容 | 呈现方式 | 与现状的关系 |
|---|---|---|---|
| **力导向图谱**（默认推荐） | 概念关系复杂的内容 | 节点按关联自动布局，边=关系，可拖动、可聚焦 | 新增，v2 设计文档提过未实现 |
| **层级树** | 概念分解、目录结构 | 现有大纲树升级为可交互层级树 | 现有大纲树视图演进 |
| **流程图** | 过程、步骤、算法 | 保留 Mermaid 渲染，增加步骤序号 | 现有 |
| **时间线** | 历史、发展脉络、演进 | 横向时间轴，节点按时间落位 | 新增 |
| **对比矩阵** | 概念对比（如 JDK vs JRE） | 行列矩阵，交叉格为对比要点 | 新增 |
| **卡片流** | 快速浏览 / 睡前速览 | 单卡片翻页，只看 label+note | 新增 |
| **Markdown / 原文** | 导出与溯源 | 保留 | 现有 |

**设计规则**：
1. 生成时由 Agent 判定 `graph_type`，**自动推荐默认视图**（如"过程类"打开即流程图），用户可随时切换
2. 视图切换只换"镜头"，不改数据——同一节点/关联在任何视图下增删改都即时同步
3. 右上角提供"视图抽屉"：一个小浮层列出全部视图 + 当前视图说明，降低探索成本

### 2.2 节点设计：三层信息结构

节点从"一行文字"升级为**三层信息结构**，支撑"速览-细读-深究"的阅读路径：

```
第 1 层 · 速览：label（15 字内）+ 类型徽标 + 掌握度色点
第 2 层 · 细读：note（具体内容/关键句/例子）——点击展开
第 3 层 · 深究：详情抽屉（note + 关联关系 + 该节点题目 + 复习记录）
```

**节点类型体系**（生成时由 LLM 标注，用于视觉区分与复习策略）：

| 类型 | 示例 | 视觉 | 复习策略 |
|---|---|---|---|
| 概念 | 过拟合 | 蓝色徽标 | 名词解释、对比题 |
| 方法/算法 | 梯度下降 | 绿色徽标 | 流程题、步骤填空 |
| 案例 | 猫狗分类例子 | 橙色徽标 | 场景应用题 |
| 公式/定义 | 交叉熵公式 | 紫色徽标 | 填空、判断 |
| 结论/规则 | 偏差方差权衡 | 灰色徽标 | 判断、关联题 |

**掌握度着色**（与视觉稿的掌握度体系打通）：
- 未学（灰）→ 学习中（蓝）→ 复习中（橙）→ 已掌握（绿）
- 力导向图中节点底色随掌握度渐变，**薄弱节点一眼可见**

### 2.3 关联设计：关系即知识

**关系类型化**（当前 relation_type 只有 related 一个值，实际是"有关系但不知道什么关系"）：

| 关系 | 语义 | 边样式建议 |
|---|---|---|
| 因果 cause | A 导致 B | 实线 + 箭头 |
| 包含 include | A 包含 B | 实线 + 空心箭头 |
| 对比 contrast | A 与 B 对立/比较 | 虚线 + 双向 |
| 递进 extends | A 是 B 的基础 | 带层级箭头 |
| 并列 parallel | 同级概念 | 细实线 |
| 依赖 depends | B 需要 A 才能理解 | 点线 |

**设计规则**：
1. 图内关联与跨图关联**统一呈现**在同一个关系网络中（对外不再区分两套体系）
2. 边可悬停显示关系说明；关系类型是复习"关联题"的数据来源
3. **聚焦模式**：点击任意节点 → 视图以该节点为中心，只显示一跳邻居 + 关系标签，其余淡出（防止大图信息过载）
4. **路径查询**：选择两个节点 → 高亮"最短关系路径"（如 过拟合→正则化→L2 范数），帮助复习时建立联想链

### 2.4 探索与编辑体验

- **节点搜索定位**：`Ctrl+F` 聚焦搜索，命中节点高亮并居中（当前完全没有定位能力）
- **新增节点联想**：输入新节点 label 时，实时联想"与此最相似/最相关"的已有节点，一键建立关系（把建关联从"手填 ID"变成"点选"）
- **编辑的安心感**：所有增删改进入可撤销栈（设计上承诺"改错了能退回去"），删除关联必须二次确认并提示"该关联用于 X 道复习题"
- **拖拽调整**：力导向视图中拖动节点微调布局；拖动到另一节点上可触发"建立关联"手势（视觉已支持拖拽，功能需跟上）

### 2.5 脉络图 ↔ 复习的联动（闭环的关键设计）

| 联动点 | 设计 |
|---|---|
| 图 → 题 | 生成脉络后自动按节点出题（见 3.2）；节点详情抽屉内直接显示"该节点的题目" |
| 题 → 图 | 复习队列中每道题都带"定位"按钮 → 跳转该节点所在脉络并高亮该节点 |
| 错 → 补救 | 复习答错 → 自动打开定位 + 展示"关联概念"面板（父概念、兄弟概念、相关概念），提示"这些相关概念也需要巩固" |
| 图 → 薄弱 | 图中节点按掌握度着色，提供"只看未掌握"过滤开关，快速形成薄弱清单 |
| 按图复习 | 每张图提供"从这张图开始复习"入口：进入该图的专属复习队列（而不是全局队列） |

---

## 三、出题复习功能设计

### 3.1 题型体系：六类题型覆盖不同认知层次

| 题型 | 认知层次 | 例子 | 生成依赖 |
|---|---|---|---|
| 名词解释 | 记忆 | "什么是过拟合？" | label + note |
| 填空 | 记忆 | "L2 正则化在损失函数中加入 ___ 惩罚项" | note 关键句挖空 |
| 判断 | 辨析 | "模型在训练集上表现好就说明泛化好（对/错）" | 易混淆点 |
| 选择 | 快速检验 | 四选一 | note + 关联概念做干扰项 |
| 关联题 | 关系理解 | "过拟合与正则化是什么关系？" | 节点的 related / 关系类型 |
| 场景应用题 | 迁移 | "训练损失降不下去且验证集更差，你会先调什么？" | note + 父节点上下文 |

**设计规则**：
- 每个节点默认 2-3 题，题型按节点类型自动分配（概念→名词解释+判断；方法→填空+场景应用）
- 选择题的**干扰项优先取自关联节点/兄弟节点**（答错的干扰项就是最容易混淆的知识点——这是出题引擎最有价值的部分）
- 难度分级：1 记忆 / 2 理解 / 3 应用，随复习表现动态调整推荐难度

### 3.2 出题引擎设计（LLM 驱动）

**出题时机**（三种，而非只在生成时）：
1. 脉络生成后自动出题（现有）
2. **节点被编辑后自动重新出题**（内容变了题就该变，当前缺失）
3. 用户手动"补题/换题"（对某题不满意，一键让 LLM 换一道）

**出题上下文**：单个节点 + note + 父节点 + 关联节点 + 关系类型 → 保证题目"有来路、有答法、有混淆点"

**质量闭环**：
- 每道题绑定"考点标签"（对应节点 id），复习统计可追溯"哪个考点最薄弱"
- 用户对某道题反复答错 → 标记该题"难"，下次出题时对该考点加大难度/增加变体
- 用户手动编辑过答案的题 → 标记 `user_edited`，LLM 重新出题时不再覆盖该题（尊重用户修正）

### 3.3 复习流程设计：学习 → 复习 → 测试 三段闭环

```
【学习】新题首学：先看题→看答案→自评"记住了/没记住"→进入队列
   ↓
【复习】到期题：回忆答案→显示答案→三档自评（忘记/模糊/认识）
   ↓（答错时）
        ┌→ 定位图节点 → 查看 note → 看关联概念 → 重答 → 过关
   ↓
【测试】阶段性测试：到期题集中连测（10 题一轮）→ 出成绩单
        → 针对错题自动生成"补救复习单"（错题 + 其关联概念题）
```

设计要点：
- **三档自评**保持（忘记/模糊/认识），符合间隔重复的交互惯例，不给用户 5 档造成决策负担
- **"学习"与"复习"分离**：新题先学后考，不能直接背（没有首学环节的队列是无效的）
- **答错不直接判死**：答错 → 给一次"查看上下文后重答"的机会；仍错才进入重学序列
- **补救复习单**是差异化学习的关键：错题的关联概念一起复习，防止"只见树木不见森林"

### 3.4 调度算法设计：完整 SM-2 → FSRS 演进

| 项 | 设计 |
|---|---|
| 间隔序列 | 完整 SM-2：1 → 6 → 16 → 45 → 90 天（由 EF 驱动），替代当前简化版 |
| 忘记处理 | 立即重考（当天 10 分钟内），连续 3 次忘记的题降级为"重学"并重新首学 |
| 每日配额 | 新题 ≤ 10 道/天 + 到期题全部（按 overdue 天数排序，最久的优先） |
| 容差窗口 | 到期 ±2 天内都算"按时复习"，避免用户因 1 天时差产生挫败感 |
| 未来演进 | 积累足够复习历史后切换到 **FSRS**（基于遗忘曲线的预测模型，Anki 采用的算法），实现真正的个性化间隔 |

### 3.5 掌握度与激励体系（与已定稿视觉稿逐点对应）

| 视觉元素（已设计） | 背后需要的数据/功能设计 |
|---|---|
| 掌握度圆环（右栏 68%） | **掌握度模型**：节点掌握度 = f(复习次数, 最近表现, 遗忘间隔)，图掌握度 = 节点加权平均；生成"掌握度"的唯一口径，全局统一 |
| 复习进度条（右栏） | 今日完成数 / 今日目标数（新题配额 + 到期题配额） |
| 今日目标胶囊（顶栏） | **每日目标系统**：默认 = 10 新题 + 当日到期题；用户可调；完成即显示勾选动画 |
| 连击胶囊（底部） | **连击系统**：每日完成目标 +1 连击；中断清零；连击数影响徽章 |
| 成就徽章（左栏 3 枚） | **成就系统**：预设里程碑——"完成第 1 张脉络""连续 7 天学习""掌握 5 个概念""单日复习 50 题""让一张图达到 80% 掌握度" |
| 学习统计卡（脉络/节点/连击） | 统计口径统一：脉络数、节点数、累计复习次数、累计答对率 |
| 本周连击胶囊（底部） | 周视图：周一到周日的完成情况点阵 |

**设计原则**：激励元素必须由真实数据驱动（掌握度、连击、目标），不允许静态占位；所有激励数据在"复习完成"事件上统一更新，前端只读接口。

**遗忘预警**：到期未复习的图在历史列表/底部胶囊显示"⚠ 3 张图有到期内容"，超过 7 天未复习的节点掌握度自动降级（真实反映遗忘，而不是永远停留在"已掌握"）。

### 3.6 数据模型设计（支撑以上功能的最小增量）

```
quiz_questions 扩展：
  + difficulty(1-3)       已有
  + tags(考点标签=节点id) 已有 node_id
  + user_edited(0/1)      新增：用户改过答案，LLM 不覆盖
  + lapses(答错次数)       新增：驱动"难"标记

review_progress 扩展：
  + streak(连续答对)      新增：驱动连击
  + lapses                新增：重学判定
  + last_rating           新增：驱动掌握度计算

新增 node_mastery：
  node_id / graph_id / score(0-100) / level(未学|学习中|复习中|已掌握) / updated_at
  —— 节点掌握度缓存表，图掌握度由它聚合

新增 daily_stats：
  date / new_done / review_done / target / streak / accuracy
  —— 驱动今日目标、连击、统计卡

新增 achievements：
  code / unlocked_at        —— 成就解锁记录

新增 study_sessions：
  id / mode(learn|review|test) / items / score / duration / started_at
  —— 驱动测试成绩单与补救复习单
```

---

## 四、用户旅程设计（两个核心场景）

### 场景 A：学习新主题（生成 → 浏览 → 出题 → 首学）

1. 输入一篇"Java 并发"笔记 → 生成脉络 → 自动进入**力导向图谱视图**，节点按类型着色
2. 浏览：点击"线程安全"节点展开 note；发现与"锁"有对比关系，聚焦查看
3. 系统提示"已为该图生成 18 道题" → 进入今日学习：先学 10 道新题（看答案 → 自评）
4. 首学完成 → 今日目标进度条推进、连击 +1、掌握度圆环出现初步数值
5. 之后每次打开，右上角"继续学习"按钮直达该图的复习队列

### 场景 B：复习日（打开 → 答错 → 定位补救 → 通关）

1. 打开应用 → 顶部"今日目标：12 题待复习"→ 点击进入复习队列
2. 第 3 题（名词解释"CAS"）答"模糊" → 系统展示答案 + "查看上下文"按钮
3. 点击 → 跳转"Java 并发"脉络图并高亮 CAS 节点 + 其关联（乐观锁、原子类）
4. 看到关联概念后"重答"过关 → 回到队列继续
5. 答完 12 题 → 成绩单："答对 10/12，CAS 相关还需巩固，已加入明日复习"
6. 底部连击 +1、本周点阵更新、掌握度圆环从 62% → 68%

---

## 五、分阶段落地（设计 → 实现顺序）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| **Phase 1：图先活起来** | 节点三层信息 + 类型徽标 + 掌握度着色；力导向图谱视图 + 视图抽屉；关系类型化 + 边样式；节点搜索定位；聚焦模式 | 节点 note 已补齐（工程项，前置） |
| **Phase 2：题先真起来** | LLM 出题引擎（六题型 + 考点标签 + 换题/补题）；学习/复习流程分离；答错定位跳转 + 关联概念面板；完整 SM-2 | Phase 1 的关联类型数据 |
| **Phase 3：学得看得见** | 掌握度模型 + 图/节点掌握度；每日目标 + 连击 + 成就徽章 + 统计卡（与视觉稿全量对接）；遗忘预警与降级 | Phase 2 的复习历史 |
| **Phase 4：进阶** | 测试模式 + 成绩单 + 补救复习单；路径查询；场景应用题进阶；FSRS 替换 | 足够复习数据 |

---

## 六、与已定稿视觉稿的对应关系（落点检查）

| 视觉稿元素 | 由本方案哪个设计支撑 | 现状 |
|---|---|---|
| 掌握度圆环 68% | 3.5 掌握度模型 + 3.6 node_mastery | 无掌握度概念，需新建 |
| 复习进度条 | 3.5 每日目标 + daily_stats | 只有"待复习 N"计数 |
| 今日目标胶囊 | 3.5 目标系统 + 3.4 每日配额 | 无目标概念 |
| 连击胶囊 | 3.5 连击系统 + review.streak | 无 |
| 成就徽章 3 枚 | 3.5 成就系统 + achievements 表 | 无 |
| 学习统计卡 | 3.5 统计口径 + daily_stats | 只有节点计数 |
| 图内节点/关联 | 2.2 节点类型着色 + 2.3 关系类型化 | 关联只有 related 一种 |
| 移动端 Tab | 视图抽屉（2.1）下沉为移动端 Tab | 无视图切换 |

> 结论：视觉稿的每个激励元素目前都**没有数据支撑**，本方案 Phase 2/3 即为它们补齐功能底座；建议视觉与功能按"Phase 1 图 + Phase 2 题"先行联调，再上激励体系。
