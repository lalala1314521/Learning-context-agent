# 学习脉络智能体 — WebUI 实施方案

> 版本: v0.1  |  日期: 2026-08-09  |  状态: 已实现 v0.1
> 二轮需求与实现计划见 [WEBUI_V2_UPDATE.md](WEBUI_V2_UPDATE.md)

## 一、目标

在现有 CLI / LangGraph / SQLite 架构之上，提供一个**分格布局**的现代化 WebUI：

- 多区块网格化界面，信息密度高，操作路径短
- 功能覆盖现有 CLI：文本/文件/URL 输入、脉络生成、节点管理、关联编辑、复习检索、长期记忆
- 前端与后端职责清晰，模块可替换、可扩展，便于后续维护
- 本地优先：默认仅监听 `127.0.0.1`；当前不需要登录，但架构上预留认证扩展点

## 二、参考图说明

本次对话中的参考图片在当前环境中无法读取。以下布局按“分格仪表盘”的通用形态设计；
如果参考图的分区不同，只需调整下方网格的区块位置与宽度，整体架构不变。

## 三、界面布局（分格网格）

```
+------------------------------------------------------------------------------+
| 顶部命令栏：Logo | 会话切换 | 联网开关 | 输出格式 | 导出 | 状态/错误提示        |
+---------------+------------------------------+-------------------------------+
| 左栏：输入与资源 | 中央：脉络画布               | 右栏：节点与关联               |
| - 文本/文件/URL | - Mermaid 渲染              | - 节点列表（层级）             |
| - 生成设置      | - Markdown 大纲切换          | - 节点详情                   |
| - 最近记录      | - 缩放 / 全屏 / 重新生成     | - 关联关系编辑               |
| - 标签筛选      |                             | - 手动增删改节点             |
+---------------+------------------------------+-------------------------------+
| 底部：长期记忆快照 | 最近操作日志 | Token/耗时统计 | 键盘快捷键提示              |
+------------------------------------------------------------------------------+
```

### 3.1 分区职责

| 区块 | 核心职责 | 关键交互 |
|------|---------|---------|
| 顶部命令栏 | 全局状态与操作 | 会话、联网开关、格式选择、导出、主题切换 |
| 左栏 | 内容输入与检索 | 文本、文件上传、URL、历史列表、搜索/回顾 |
| 中央画布 | 脉络可视化 | Mermaid 渲染、大纲切换、缩放、重新生成 |
| 右栏 | 节点管理 | 节点树、详情、新增/修改/删除、关联关系 |
| 底部栏 | 记忆与观测 | 记忆快照、操作日志、Token 与耗时统计 |

### 3.2 响应式规则

- 桌面宽屏（>= 1280px）：五区块网格全展开，左栏 320px，右栏 360px
- 笔记本（900-1279px）：左右栏收窄，底部栏折叠为可展开面板
- 平板/移动（< 900px）：单列滚动布局，区块依次堆叠，画布优先
- 网格使用 CSS Grid + 固定/最小/最大轨道宽度，避免内容挤压与重叠

### 3.3 视觉风格：纸感 × 复古胶卷

- 基底：暖米白 / 旧纸色背景，墨色正文，棕褐与暗红强调色，避免高饱和荧光色
- 分格：区块像“纸卡 / 胶片格”，细边框 + 轻微纸张纹理；画布像“放映画面”，
  可带微颗粒、暗角与上下留黑边（保留开关，默认轻度）
- 装饰：胶卷孔、日期章、手写感标签作为低干扰点缀，不遮挡内容
- 字体：标题用衬线/仿宋感字体，正文用易读无衬线或系统衬线，数字/代码用等宽字体
- 主题：浅色“旧纸”与深色“暗房”两套，通过 CSS 变量整体切换

### 3.4 阅读舒适性

- 正文栏宽不超过 70ch，节点文本行宽不超过 40ch
- 行高 1.6-1.8，正文不小于 15px，对比度满足 WCAG AA
- 纸张纹理只作背景点缀，文字区域使用近纯色底
- 表格/列表行距充足，hover 状态清晰但不闪烁
- 长内容默认折叠或分页，避免单屏堆满

## 四、技术选型

| 层 | 推荐方案 | 理由 |
|----|---------|------|
| 后端框架 | FastAPI + Uvicorn | 类型安全、自动 OpenAPI、易扩展 API |
| 数据校验 | Pydantic v2 | 与 FastAPI 原生集成 |
| 前端 | 原生 ES Modules + CSS Variables | 无构建链、依赖少、易于维护；后续可迁 Vue |
| 页面渲染 | Jinja2 模板 + 静态资源 | 首屏快，路由简单 |
| 图表 | Mermaid.js（本地 vendor） | 与现有 Mermaid 输出天然一致 |
| 状态管理 | LangGraph checkpoint + SQLite | 复用现有架构 |
| 测试 | pytest + Playwright | API 单测 + UI 冒烟/截图 |

> 前端暂不引入 Vue/React 和打包器，避免项目从“轻量工具”过早变成“前端工程”；
> 若后续界面复杂度显著上升，再引入 Vite + Vue，组件边界已按此预留。

## 五、目录结构（扩展后）

```
app/
├── web/
│   ├── __init__.py
│   ├── server.py            # uvicorn 入口
│   ├── api.py               # /api/v1 路由
│   ├── schemas.py           # Pydantic 请求/响应模型
│   ├── dependencies.py      # 数据库 / 图 / 工具依赖注入
│   ├── security.py          # 认证占位层（当前关闭，未来接账号/token）
│   ├── static/
│   │   ├── css/app.css      # 设计令牌 + 网格布局
│   │   ├── js/app.js        # 入口与事件总线
│   │   ├── js/api.js        # API 客户端
│   │   ├── js/components/   # 分格组件（按 UI 分区拆分）
│   │   ├── vendor/mermaid.min.js
│   │   └── img/             # 纸张纹理、颗粒等静态素材
│   └── templates/index.html
├── graph/                   # LangGraph 逻辑（保持不变）
├── tools/                   # 工具层（保持不变）
├── memory/                  # Repository 层（保持不变）
└── data/
    └── uploads/             # 文件上传临时目录（运行时生成）
```

## 六、API 设计（v1 草案）

| 方法 | 路径 | 用途 |
|------|------|------|
| `POST` | `/api/v1/graphs/generate` | 文本/命令输入，生成并保存脉络 |
| `POST` | `/api/v1/graphs/parse` | 文件/URL 预解析，返回文本 |
| `GET` | `/api/v1/graphs` | 列表 + 关键词检索 |
| `GET` | `/api/v1/graphs/{id}` | 图详情 + 节点 |
| `DELETE` | `/api/v1/graphs/{id}` | 删除图 |
| `POST` | `/api/v1/nodes` | 新增节点 |
| `PATCH` | `/api/v1/nodes/{id}` | 修改节点/关联/排序 |
| `DELETE` | `/api/v1/nodes/{id}` | 删除节点 |
| `GET` | `/api/v1/memories` | 长期记忆快照 |
| `GET` | `/api/v1/sessions/{id}` | 会话状态/历史 |
| `GET` | `/api/v1/health` | 健康检查 |

统一返回 `{ "ok": bool, "data": ..., "error": ... }`，接口版本号从 `/api/v1` 开始，
后续破坏性变更只新增 `/api/v2`，保证可维护性。

## 七、功能清单

### 7.1 输入与生成

- [x] 文本输入生成脉络
- [x] 文件上传（拖拽或选择，TXT/MD/PDF/DOCX/IPYNB），后端解析后回显可编辑文本
- [x] 文件大小/类型校验、解析失败可读提示
- [x] URL 抓取，后端解析正文后回显
- [ ] 批量文件/URL（沿用 `files:` / `urls:` 语义）
- [x] 联网搜索开关
- [x] 输出格式：Mermaid / Markdown / 两者
- [ ] 生成长度/Token 预算提示

### 7.2 画布与预览

- [x] Mermaid 渲染（本地 vendor，离线可用）
- [ ] Mermaid 语法错误重试（同参数重新生成，最多 2 次）
- [x] Markdown 大纲 / Mermaid 视图切换
- [x] 导出 Mermaid/Markdown/JSON
- [ ] 缩放、全屏、导出 PNG/SVG
- [ ] 节点 hover 高亮关联

### 7.3 节点与关联

- [x] 节点树列表（父/子层级）
- [x] 节点详情：label、note、parent、related
- [x] 手动新增/修改/删除节点
- [x] 关联节点编辑（`related_nodes`）
- [ ] 撤销/重做（前端本地栈，至少 20 步）

### 7.4 记忆与复习

- [x] 最近脉络历史（时间倒序）
- [x] 关键词搜索 / `回顾 <主题>` 入口
- [x] 长期记忆快照面板
- [ ] 标签筛选（后续扩展）

### 7.5 体验

- [x] 深色/浅色主题（浅色“旧纸” + 深色“暗房”）
- [x] 键盘快捷键（Ctrl+Enter 生成、Enter 检索）
- [x] 操作日志与错误提示（toast / 状态栏）
- [x] 空状态、加载状态、错误状态
- [ ] 阅读设置：字号、行距、正文栏宽调节

## 八、可扩展性设计

1. **前端组件化**：每个 UI 分区对应 `js/components/` 下一个模块，只通过事件总线通信，
   新增功能时优先“加组件”而不是“改大文件”。
2. **后端模块化**：API 路由按资源拆分；输入解析、生成、管理分别走独立 service，
   WebUI 不直接调用 LangGraph State 细节。
3. **设计令牌**：颜色、间距、圆角、阴影统一为 CSS 变量，主题切换只改变量。
4. **工具注册表**：未来新增输入源（Notion、图片 OCR 等）注册到 `input_sources`，
   前端自动出现对应入口。
5. **API 版本化 + OpenAPI**：FastAPI 自动生成接口文档，前端 API 客户端从 schema 对齐。
6. **测试分层**：repository 单测、API 集成测试、UI 冒烟测试分目录维护。
7. **认证可插拔**：`security.py` 当前为空实现；未来只需在该层接入 token 校验或
   用户表，业务路由不改动即可启用登录。

## 九、建议的优化项

### 9.1 可靠性

- Mermaid 语法校验：解析失败时携带错误信息重新生成一次
- LLM 请求重试与超时：使用指数退避，连续失败后给出可读错误
- 长文本分块：超过阈值时先摘要再生成脉络
- 文件/URL 解析放入后台任务，避免阻塞 UI

### 9.2 性能

- 生成过程使用 SSE 流式输出，前端逐段渲染
- 列表接口只返回摘要字段，详情再拉全文
- SQLite 增加 FTS5 全文索引，提升中文关键词检索
- 图片/大文件解析结果缓存

### 9.3 数据与安全

- 默认仅监听 `127.0.0.1`；当前不启用登录，预留认证层；如未来暴露到局域网，
  启用简单口令或账号体系即可
- 文件上传限制大小与类型（默认单文件 <= 20MB），上传文件存 `data/uploads/`，
  解析后可选清理，避免残留敏感文件
- 敏感内容脱敏提示：发送给 LLM 前提示用户
- 数据库备份入口：导出 SQLite 或 JSON

### 9.4 体验

- 节点拖拽调整父子关系，保存后回写 `parent_id`
- 画布右键菜单：复制节点、添加子节点、建立关联
- 多会话管理：每个会话独立 checkpoint thread_id
- 生成结果与用户手动修改版本对比

## 十、实施阶段

### Phase A：骨架与布局

- [x] 引入 `fastapi` / `uvicorn`，搭建 `app/web/` 目录
- [x] 完成分格 HTML/CSS 布局、纸感/复古胶卷设计令牌与双主题
- [x] 建立 `data/uploads/` 与上传校验、`security.py` 占位层
- [x] 完成 `/api/v1` 基础路由与 Pydantic schema

### Phase B：核心生成链路

- [x] 文本生成脉络，Mermaid 渲染
- [x] 列表、搜索、打开图详情
- [x] 联网开关与输出格式

### Phase C：节点管理与记忆

- [x] 节点 CRUD 与关联编辑
- [x] 长期记忆快照面板
- [x] 历史记录
- [ ] 标签筛选

### Phase D：打磨与测试

- [x] 响应式、主题、快捷键、导出
- [x] pytest API 测试
- [ ] Playwright UI 冒烟
- [ ] 性能与错误处理优化

## 十一、验收标准

- 五区块分格 UI 在桌面/笔记本/移动端均无重叠、无溢出
- 从输入文本到 Mermaid 渲染、保存、检索、节点修改全链路可用
- 文件可拖拽上传、解析回显，解析失败给出可读错误
- 浅色“旧纸”与深色“暗房”主题均可切换，正文阅读无纹理干扰
- 无网络依赖（除 DeepSeek/Tavily 调用本身）即可启动并查看历史
- 新增一个 UI 分区不需要改动其他分区核心逻辑
- 接口全部通过 `/api/v1` 版本化管理

## 十二、待确认事项

- 参考图的实际分区布局（当前按通用分格仪表盘设计）
- 文件上传与 `file:` 路径输入都会支持（已确认）
- 登录当前不做，仅预留扩展层（已确认）
- 是否接受新增 `fastapi` + `uvicorn` 依赖（默认接受，需最终确认）

## 十三、实现记录（2026-08-09）

### 已完成

- `app/web/`：FastAPI 应用、`/api/v1` 路由、Pydantic schema、依赖注入、认证占位层
- 前端：五区网格布局、纸感/暗房双主题、文件拖拽上传、URL 抓取、Mermaid 渲染、
  Markdown 大纲切换、节点 CRUD、关联编辑、历史检索、长期记忆面板、导出
- Mermaid 已下载到 `static/vendor/mermaid.min.js`，离线可用
- 测试：新增 `tests/test_web_api.py`，连同原有测试共 15 个用例全部通过
- 真实端到端：WebUI 服务运行在 `http://127.0.0.1:8000`，通过接口生成并保存了
  一张含 15 个节点的脉络图，长期记忆快照同步写入

### 后续可迭代

- 批量文件/URL 的 UI 入口与进度展示
- SSE 流式生成、Mermaid 错误重试
- 节点拖拽、撤销/重做、标签筛选
- 阅读设置面板、PNG/SVG 导出
- 登录体系（`security.py` 扩展点已预留）
